// extern crate common;

use common::application::Application;

fn main() {
    println!("Hello, world!");

    let app = Application::new();
    // for (key, value) in app.registry.providers {

    // }
}
